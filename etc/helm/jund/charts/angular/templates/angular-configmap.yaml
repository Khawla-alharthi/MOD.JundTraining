apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-configmap
data:
  dynamic-env.json: |-
    {
        "production": "true",
        "application": {
            "baseUrl": "{{ include "jund.hosts.angular" . }}",
            "name": "Jund",
            "logoUrl": ""
        },
        "oAuthConfig": {
            "issuer": "{{ include "jund.hosts.httpapi" . }}",
            "redirectUri": "{{ include "jund.hosts.angular" . }}",
            "requireHttps": "false",
            "clientId": "Jund_App",
            "responseType": "code",
            "scope": "offline_access Jund",
            "strictDiscoveryDocumentValidation": false,
            "skipIssuerCheck": true
        },
        "apis": {
            "default": {
                "url": "{{ include "jund.hosts.httpapi" . }}",
                "rootNamespace": "MOD.Jund"
            }
        }
    }